# Production environment example for Vercel
# NOTE: Do NOT commit real secrets. Use your Vercel project settings to add values.

# MongoDB (Atlas) connection string
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/galaxy_securities?retryWrites=true&w=majority

# NextAuth
NEXTAUTH_SECRET=replace_with_secure_secret
NEXTAUTH_URL=https://<your-vercel-domain>

# Feature toggles
USE_REAL_DB=true
NODE_ENV=production
